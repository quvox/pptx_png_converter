version: '3.8'

services:
  pptx-converter:
    build: .
    volumes:
      - ./test/input:/app/input
      - ./test/output:/app/output
    environment:
      - TZ=Asia/Tokyo
    restart: unless-stopped
    # ホストのディレクトリ変更をコンテナ内で検知できるようにする
    privileged: true