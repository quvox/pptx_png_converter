このツールの開発方法について
====


このツールは、VS Code + Roo codeを用いて、設計から実装までの全ての工程をAIで実施した。


## 経過

1. PPTXのスライドをPNGに変換するツールをDocker上に作成することをリクエストした
  * シェルスクリプトでの実装を提案された

2. 何度もAIとやりとりしたが、安定性が上がらなかった
  * ファイル変更を重複検知してしまい、不要な変換処理が頻発する

3. ファイル変更検知の処理だけ、Pythonで実装するよう指示を出した（これは人間の判断による）

4. 何度かのやり取りで、チューニングも行うことで、希望通りに動作するようになった
  * 出力PNGファイルが存在しない場合に、PPTXを開いた瞬間にPNGが出力されてしまう仕様になってしまっているが、それは仕方なしとして諦めた
  * 出力するPNGの画像の外側に5%のマージンを含めるよう指示したところ、あっさり成功した

5. 最後に、これまでのやり取り（タスク）を、逆にドキュメント（implementation-plan.md）に反映するように指示した



なお、この間、ソースコードやマークダウンドキュメントは、直接は一切触っていない。

